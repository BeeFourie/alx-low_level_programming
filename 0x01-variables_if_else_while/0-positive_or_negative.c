#include <stdio.h>

int main() {
	    double num;

	        printf("Next()");

		    scanf("%lf", &num);

		        if (num <= 0.0) {
				     if (num == 0.0)
			printf("is zero");
				     
					else

			printf("is negative");

   } 

				    else

			printf("is positive");

			    	return (0);

}
