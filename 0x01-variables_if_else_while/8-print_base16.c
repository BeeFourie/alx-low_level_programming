#include <stdio.h>
/**
* main - Entry point
* Description: 'program that prints all the numbers of base 16 in lowercase'
*Return: Always 0 (Success)
*/
int main()
{
char i;
scanf("%x", &num);
char ch;
for(ch = 'a' ; ch <= 'z' ; ch++) {
putchar("%x",ch);
}
return (0);
}
